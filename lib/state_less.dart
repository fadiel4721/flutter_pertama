// ignore_for_file: camel_case_types

import 'package:flutter/material.dart';

class latihan_State extends StatelessWidget {
  const latihan_State({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
